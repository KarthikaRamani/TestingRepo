apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: pod-name
  name: pod-name
spec:
  replicas: 2
  selector:
    matchLabels:
      run: pod-name
  template:
    metadata:
      labels:
        run: pod-name
    